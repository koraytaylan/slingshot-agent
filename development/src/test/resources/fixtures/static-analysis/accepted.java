package rs.slingshot.agent.fixture;

/** A file that switches nothing off and mentions every form only as text. */
public final class Accepted {

    /** The forms this repository refuses, named here as data rather than used as annotations. */
    public static final String REFUSED = "@SuppressWarnings @SuppressFBWarnings CHECKSTYLE:OFF NOPMD";

    private Accepted() {
    }
}
