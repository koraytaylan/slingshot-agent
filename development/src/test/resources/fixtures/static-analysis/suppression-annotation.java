package rs.slingshot.agent.fixture;

/** A file that switches a rule off with an annotation. */
public final class SuppressionAnnotation {

    /**
     * Does nothing, loudly.
     */
    @SuppressWarnings("unchecked")
    public void act() {
    }
}
