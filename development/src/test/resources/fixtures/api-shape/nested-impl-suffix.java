package rs.slingshot.agent.fixture;

/** A type holding one whose name says a second name was needed. */
public final class NestedImplSuffix {

    private NestedImplSuffix() {
    }

    /** A nested type with the refused suffix. */
    static final class InnerImpl {
    }
}
