package rs.slingshot.agent.fixture;

/** A file whose declared name spells a word short. */
public final class AbbreviatedName {

    /**
     * Answers nothing in particular.
     *
     * @param reqCount how many requests
     * @return the count
     */
    public static int count(int reqCount) {
        return reqCount;
    }
}
