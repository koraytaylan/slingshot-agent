# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 Koray Taylan Davgana

[scenario]
id = "terminate-workflow-instance"
kind = "command"
feature = "terminate_workflow_instance"
tier = "a"
deployments = ["aem-cloud-service", "aem-6-5-lts"]
runner = "rs.slingshot.agent.interop.tier.TerminateWorkflowInstanceScenario"
reason = "A control a deployment may refuse outright. What this proves that no unit suite can: that its row requires an operation key, that it declares the platform control's own unknown outcome rather than the repository mutation's, and that the surface is reachable through the transport."
