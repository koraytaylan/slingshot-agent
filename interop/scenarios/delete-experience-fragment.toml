# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 Koray Taylan Davgana

[scenario]
id = "delete-experience-fragment"
kind = "command"
feature = "delete_experience_fragment"
tier = "a"
deployments = ["aem-cloud-service", "aem-6-5-lts"]
runner = "rs.slingshot.agent.interop.tier.DeleteExperienceFragmentScenario"
reason = "A command that removes an experience fragment, on a running instance. What this proves that no unit suite can: that its row requires an operation key, that it declares both the answer nobody knows and the refusal for a fragment something still points at, and that the write surface is reachable through the transport at all."
