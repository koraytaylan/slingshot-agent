# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 Koray Taylan Davgana

[scenario]
id = "set-user-disabled"
kind = "command"
feature = "set_user_disabled"
tier = "a"
deployments = ["aem-cloud-service", "aem-6-5-lts"]
runner = "rs.slingshot.agent.interop.tier.SetUserDisabledScenario"
reason = "The one change here that can lock somebody out of their own instance, which is why the state is a name rather than a flag on this side. What this proves that no unit suite can: that its row declares the platform control's own unknown outcome rather than the repository mutation's, and that it requires an operation key."
