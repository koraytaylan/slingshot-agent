# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 Koray Taylan Davgana

[scenario]
id = "delete-page"
kind = "command"
feature = "delete_page"
tier = "a"
deployments = ["aem-cloud-service", "aem-6-5-lts"]
runner = "rs.slingshot.agent.interop.tier.DeletePageScenario"
reason = "A command that changes a repository, on a running instance. What this proves that no unit suite can: that its row requires an operation key and a submission carrying none is refused before anything runs, that the row declares the answer nobody knows so an interrupted commit is not reported as a failure, and that the write surface is reachable through the transport at all."
