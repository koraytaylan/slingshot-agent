# SPDX-License-Identifier: MIT OR Apache-2.0
# Copyright 2026 Koray Taylan Davgana
#
# Every value here is the client's own, from its published catalogue and its published schemas.

[command]
wire_name = "delete_asset"
contract_version = "1.0.0"
access = "write"

# Not intrinsically idempotent: running it twice is not running it once, so the caller supplies a
# key and this side answers the first attempt's own result to every resend of it.
operation_key = "required"

result_bytes = 16384
failure_categories = ["asset_access_denied", "asset_invalid", "asset_is_referenced", "asset_not_found", "deletion_budget_exceeded", "mutation_outcome_unknown", "repository_commit_failed"]
argument_schema_digest = "6cf43cf657acc219531325e60900d66370d1a313a9f0632ee21c7c2e14e1aa34"
result_schema_digest = "14b2c3dea377d1d49c3bde963c74c5e178da773df618e936b86b1ee377a27323"
contract_limits_digest = "c1d75299a29c1f4ab3495e5fff3f04bf483475573582d2976cfdf018fe46c255"
staging_bytes = 0
execution = "immediate"
